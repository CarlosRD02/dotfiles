(defwidget workspaces []
  (box :class "workspaces"
       :spacing 4

    (for ws in workspaces
      (button
        :class {ws.active ? "ws ws-active" : "ws"}
        :onclick "bspc desktop -f ${ws.name}"

        (label
          :text {ws.name})))))
